{
  "name": "mgpic-2025",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run moon:build && npm run moon:copy && vite build",
    "build:web": "vite build",
    "preview": "vite preview",
    "moon:build": "cd main && moon build --target js",
    "moon:copy": "cp main/target/js/release/build/main/main.js src/main.js",
    "moon:dev": "npm run moon:build && npm run moon:copy && npm run dev",
    "deploy:assets": "node scripts/upload-assets.js",
    "deploy:assets:smart": "node scripts/smart-upload.js",
    "deploy:full": "npm run build && npm run deploy:assets:smart",
    "test:upload-config": "node scripts/test-upload-config.js"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "@vitejs/plugin-vue": "^5.0.0",
    "chokidar": "^3.5.3",
    "vite": "^5.0.0",
    "dotenv": "^16.0.0"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "three": "^0.180.0",
    "vue": "^3.5.21"
  }
}
